# Segredos exclusivos do plano administrativo do Storage compartilhado.
# Este arquivo nunca deve ser injetado em containers de projeto.
# openssl rand -hex 32
SERVER_ADMIN_API_KEYS=pass
# openssl rand -hex 32
AUTH_ENCRYPTION_KEY=pass
